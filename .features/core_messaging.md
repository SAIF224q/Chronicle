# Feature: Core Messaging Interface

## Overview
Chronicle provides a baseline messaging-style journaling layout. Instead of traditional form-based diary entries, the interface mimics a clean, conversational timeline where entries flow chronologically.

## Key UX Elements
- Self-chat layout where the user writes and logs entries as messages.
- Clean chronological timeline stream.
- Support for inline tag extraction (`#hashtag`).
